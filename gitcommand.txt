Daily Workflow

git add .
git commit -m "comments here"
git push

If you need to restore project on another computer

git clone https://github.com/CoryM-WebDeveloper/GtaVModManager.git

To setup an initial git repository:

git init
git add .
git commit -m "Initial project setup"

On github:
1. Click New repository
2. Name it <Program Name Here no spaces>
3. Choose Public or Private
4. Do not initialize with a README or .gitignore.
- This will be done within VSCode and committed later.
5. Click Create Repository.

If you didn't read step 4 and are having difficulties where the online
version is ahead of the local version, complete the following steps:

git pull origin main --allow-unrelated-histories
git commit -m "Merge remote changes"
git push origin main

Then finish by:
git remote add origin https://github.com/CoryM-WebDeveloper/GtaVModManager.git
git branch -M main
git push -u origin main
git push -u origin main

Now Ready for Daily Workflow above.

New Quick Summary:
git init
git add .
git commit -m "Initial project setup"
git remote add origin https://github.com/YourUsername/GtaVModManager.git
git branch -M main
git push -u origin main

🧭 Recommended Branch Setup
✔ Keep main = stable baseline
- Always runnable
- Always “clean mod manager core”
- Never break it intentionally

✔ Create dev (or develop) = active work zone
- New features go here
- Broken builds are allowed
- Rapid iteration happens here

How To:

git checkout -b dev
git push -u origin dev

🧠 How your workflow now works
On dev (your workshop 🔧)
- Add features
- Break things
- Refactor systems
- Build mod deployment engine

When something is stable:

git checkout main
git merge dev

Then push:

git push



🛡️ Extra safety (highly recommended)

You can also protect main on GitHub:

Go to:

- GitHub repo → Settings → Branches
- Add branch protection rule for main
    - Require pull requests
    - Block direct pushes (optional but powerful)

This turns main into a sealed “release vault.”