🧭 PHASE 2 — MOD DISCOVERY SYSTEM (REBUILT CLEAN)
🎯 Goal of this phase

By the end of Phase 2, your app will:

Scan Mods/ folder
Detect mod folders
Convert them into Mod objects
Display them in WPF UI list
Show live status: “Discovered X mods”
📁 REQUIRED FOLDER STRUCTURE

Make sure your project root looks like this:

GtaVModManager/
├── Mods/
│   ├── TestMod1/
│   ├── TestMod2/
├── Cache/
├── Logs/
├── Profiles/
├── src/
    └── GtaVModManager/

👉 IMPORTANT: Mods folder must be at project root, NOT inside src.

🧱 STEP 1 — Create Mod Model

Create:

Models/Mod.cs
namespace GtaVModManager.Models;

public class Mod
{
    public string Name { get; set; } = string.Empty;
    public string FullPath { get; set; } = string.Empty;
    public bool IsEnabled { get; set; } = true;
}
🔍 STEP 2 — Create ModDiscoveryService

Create:

Services/ModDiscoveryService.cs
using System.IO;
using System.Collections.Generic;
using System.Linq;
using GtaVModManager.Models;

namespace GtaVModManager.Services;

public class ModDiscoveryService
{
    private readonly string _modsPath;

    public ModDiscoveryService(string modsPath)
    {
        _modsPath = modsPath;
    }

    public List<Mod> GetMods()
    {
        if (!Directory.Exists(_modsPath))
            return new List<Mod>();

        var modDirectories = Directory.GetDirectories(_modsPath);

        return modDirectories.Select(dir => new Mod
        {
            Name = Path.GetFileName(dir),
            FullPath = dir,
            IsEnabled = true
        }).ToList();
    }
}
🧠 STEP 3 — Create MainViewModel

Create / replace:

ViewModels/MainViewModel.cs
using System.Collections.ObjectModel;
using CommunityToolkit.Mvvm.ComponentModel;
using GtaVModManager.Models;
using GtaVModManager.Services;

namespace GtaVModManager.ViewModels;

public partial class MainViewModel : ObservableObject
{
    private readonly SettingsService _settingsService;

    [ObservableProperty]
    private string statusText = "Initializing...";

    [ObservableProperty]
    private ObservableCollection<Mod> mods = new();

    public MainViewModel()
    {
        _settingsService = new SettingsService();

        LoadMods();
    }

    private void LoadMods()
    {
        var modService = new ModDiscoveryService("Mods");

        var discovered = modService.GetMods();

        Mods.Clear();

        foreach (var mod in discovered)
            Mods.Add(mod);

        StatusText = $"Discovered {Mods.Count} mods.";
    }
}
🪟 STEP 4 — App.xaml.cs (SIMPLE VERSION)
using System.Windows;
using GtaVModManager.ViewModels;

namespace GtaVModManager;

public partial class App : Application
{
    protected override void OnStartup(StartupEventArgs e)
    {
        base.OnStartup(e);

        var window = new MainWindow
        {
            DataContext = new MainViewModel()
        };

        window.Show();
    }
}
🧾 STEP 5 — App.xaml

Make sure it is CLEAN:

<Application x:Class="GtaVModManager.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
</Application>

🚫 No StartupUri.

🪟 STEP 6 — MainWindow.xaml
<Window x:Class="GtaVModManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="GTA V Mod Manager"
        Height="600"
        Width="1000">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="GTA V Mod Manager"
                   FontSize="28"
                   FontWeight="Bold"/>

        <TextBlock Grid.Row="2"
                   Text="{Binding StatusText}"
                   FontSize="14"/>

        <ListBox Grid.Row="4"
                 ItemsSource="{Binding Mods}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <CheckBox IsChecked="{Binding IsEnabled}" Margin="5"/>
                        <TextBlock Text="{Binding Name}" FontSize="14"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

    </Grid>
</Window>
🧪 STEP 7 — Create test mods

Inside:

Mods/

Create folders:

TestMod1/
TestMod2/

(empty folders are fine)

🚀 STEP 8 — Run

From:

dotnet run
🎯 EXPECTED RESULT

You should see:

UI:
Title
“Discovered 2 mods”
List showing:
TestMod1
TestMod2
Checkboxes next to each
🧠 WHAT PHASE 2 GIVES YOU

You now have:

✔ File system scanning engine
✔ Mod abstraction model
✔ MVVM binding pipeline
✔ Observable UI updates
✔ First real “data layer”

Phase 2 SUCCESSFUL